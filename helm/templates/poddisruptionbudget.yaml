{{- if and .Values.controllerManager.pdb.enabled (gt (int .Values.controllerManager.replicas) 1) }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "oracle-database-operator.name" . }}-controller-manager
  namespace: {{ include "oracle-database-operator.namespace" . }}
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
spec:
  {{- if .Values.controllerManager.pdb.minAvailable }}
  minAvailable: {{ .Values.controllerManager.pdb.minAvailable }}
  {{- else if .Values.controllerManager.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.controllerManager.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: 1
  {{- end }}
  selector:
    matchLabels:
      {{- include "oracle-database-operator.selectorLabels" . | nindent 6 }}
{{- end }}
