{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "oracle-database-operator.clusterName" . }}-proxy-rolebinding
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "oracle-database-operator.clusterName" . }}-proxy-role
subjects:
- kind: ServiceAccount
  name: {{ include "oracle-database-operator.serviceAccountName" . }}
  namespace: {{ include "oracle-database-operator.namespace" . }}
{{- if eq .Values.scope.mode "cluster" }}
---
# ClusterRoleBinding for cluster-scoped deployment (watch all namespaces)
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "oracle-database-operator.clusterName" . }}-manager-rolebinding
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "oracle-database-operator.clusterName" . }}-manager-role
subjects:
- kind: ServiceAccount
  name: {{ include "oracle-database-operator.serviceAccountName" . }}
  namespace: {{ include "oracle-database-operator.namespace" . }}
{{- end }}
{{- end }}
