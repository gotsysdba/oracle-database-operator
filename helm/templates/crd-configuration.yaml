{{- if .Values.crdConfiguration.enabled }}
{{- $name := printf "%s-crd-configuration" (include "oracle-database-operator.name" .) -}}
{{- $clusterName := printf "%s-crd-configuration" (include "oracle-database-operator.clusterName" .) -}}
{{- $crds := list -}}
{{- $conversionCrds := list -}}
{{- range $path, $_ := .Files.Glob "crds/*.yaml" -}}
{{- $crd := fromYaml ($.Files.Get $path) -}}
{{- $crds = append $crds $crd.metadata.name -}}
{{- if eq (dig "conversion" "strategy" "" $crd.spec) "Webhook" -}}
{{- $conversionCrds = append $conversionCrds $crd.metadata.name -}}
{{- end -}}
{{- end -}}
{{- $account := default $name .Values.crdConfiguration.serviceAccount.name -}}
{{- if .Values.crdConfiguration.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
  name: {{ $account }}
  namespace: {{ include "oracle-database-operator.namespace" . }}
---
{{- end }}
{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
  name: {{ $clusterName }}
rules:
- apiGroups: [apiextensions.k8s.io]
  resources: [customresourcedefinitions]
  resourceNames:
  {{- range $crds }}
  - {{ . }}
  {{- end }}
  verbs: [get, patch]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
  name: {{ $clusterName }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ $clusterName }}
subjects:
- kind: ServiceAccount
  name: {{ $account }}
  namespace: {{ include "oracle-database-operator.namespace" . }}
---
{{- end }}
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    {{- include "oracle-database-operator.labels" . | nindent 4 }}
  name: {{ $name }}
  namespace: {{ include "oracle-database-operator.namespace" . }}
  annotations:
    helm.sh/hook: post-install,post-upgrade
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  backoffLimit: 3
  activeDeadlineSeconds: 180
  template:
    spec:
      restartPolicy: Never
      serviceAccountName: {{ $account }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      initContainers:
      - name: certificate-references
        image: {{ .Values.crdConfiguration.image | quote }}
        imagePullPolicy: {{ .Values.crdConfiguration.imagePullPolicy }}
        resources:
          {{- toYaml .Values.crdConfiguration.resources | nindent 10 }}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: [ALL]
        command: [kubectl]
        args:
        - patch
        {{- range $crds }}
        - customresourcedefinition/{{ . }}
        {{- end }}
        - --type=merge
        - --patch
        - {{ dict "metadata" (dict "annotations" (dict "cert-manager.io/inject-ca-from" (include "oracle-database-operator.certManagerInjectAnnotation" .))) | toJson | quote }}
      containers:
      - name: conversion-references
        image: {{ .Values.crdConfiguration.image | quote }}
        imagePullPolicy: {{ .Values.crdConfiguration.imagePullPolicy }}
        resources:
          {{- toYaml .Values.crdConfiguration.resources | nindent 10 }}
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop: [ALL]
        command: [kubectl]
        args:
        - patch
        {{- range $conversionCrds }}
        - customresourcedefinition/{{ . }}
        {{- end }}
        - --type=merge
        - --patch
        - {{ dict "spec" (dict "conversion" (dict "webhook" (dict "clientConfig" (dict "service" (dict "name" (include "oracle-database-operator.webhookServiceName" .) "namespace" (include "oracle-database-operator.namespace" .)))))) | toJson | quote }}
{{- end }}
